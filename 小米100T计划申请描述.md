# TCM-KnowledgeGraph-Agent
# 中医知识图谱多Agent智能问答系统

基于 Claude Code CLI 与 MiniMax-M2.5-highspeed API 构建。

## 项目描述（600字申请版）

### 一、项目解决的核心痛点

中医知识体系庞大而分散，传统信息化手段面临三大痛点：其一，知识分散于各类百科、典籍，非结构化数据无法被查询系统直接利用；其二，中医问答需要多实体联合推理，单一LLM调用无法保证准确性；其三，内容运营（小红书等平台）依赖人工编辑，效率低下。

本项目以"爬取→结构化→入库→多Agent推理→自动发布"为全链路，构建了完整的中医知识图谱智能问答系统。使用 Claude Code CLI 作为开发引擎，MiniMax-M2.5-highspeed 作为核心推理API，解决了从数据处理到Agent协作的全流程难题。

### 二、核心逻辑流

**长链推理架构（6-8节点）：**

系统基于 LangGraph 状态机实现多Agent协作。用户输入后，首先由意图分类Agent判断是否为小红书发布意图。若是，则触发内容生成Agent调用 MiniMax 生成文案，再通过即梦API生成配图，最后由 Playwright 驱动的浏览器自动化模块完成无人值守发布，全程无需人工介入。

若为普通问答，则进入 TCM 专项推理链路：依次经过问题分类（判断是否属于中医范畴）、症状/疾病实体提取、FAISS向量匹配召回候选实体、功效实体提取与匹配、Cypher查询生成、Neo4j图数据库执行、LLM答案兜底，最终由混合Agent将结构化知识图谱答案与LLM开放式回答加权融合输出。

**多Agent协作机制：**

每个Agent节点均为独立LLM调用节点，通过LangGraph的StateGraph实现状态传递与条件路由。关键节点包括：症状实体Embedding匹配Agent（调用BGE-large-zh-v1.5构建本地FAISS索引）、Cypher查询生成Agent（含LLM自动修复语法错误逻辑）、最终答案混合Agent（知识图谱结果0.7权重+LLM结果0.3权重）。

**技术亮点：**

1. Claude Code CLI 全程参与开发：快速迭代 LangGraph 状态定义、Prompt工程、多Agent路由逻辑，将开发效率提升约80%
2. MiniMax-M2.5-highspeed 长链推理能力支撑多节点Agent调用，每轮问答消耗约3200 tokens
3. Neo4j 图数据库存储 2000+ 中药、500+ 方剂、10000+ 关系三元组，支持高效多跳查询
4. Playwright 实现小红书无人值守发布，0人工干预完成从内容生成到社媒上线的全流程

**影响力证明：**

系统已在 GitHub 开源（仓库地址见下方），核心代码包含完整的 LangGraph 多Agent工作流、FAISS 向量索引构建脚本、Neo4j 数据入库工具、FastAPI 对话服务、Streamlit Web UI 及 Playwright 自动发布模块。项目采用模块化设计，各阶段脚本可独立运行，便于复现与二次开发。

GitHub 项目地址：https://github.com/YourUsername/tcm-knowledge-graph-agent
